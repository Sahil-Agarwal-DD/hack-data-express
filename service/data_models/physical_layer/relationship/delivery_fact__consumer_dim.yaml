join_definition:
  from_entity: delivery_fact
  to_entity: consumer_dim
  cardinality_type: OneToMany
  join_key:
     from_entity_key: delivery_fact.creator_id
     to_entity_key: consumer_dim.consumer_id
  join_type: LEFT  # Type of join